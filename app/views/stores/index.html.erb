<h1> From Blake's Apartment </h1>
<ul>
<% @items.each do |item| %>
    <li>
      <%= link_to item.item_name, item_path(item) %> -
      <%= link_to "See Item Offers", item_offers_path(item) %>
    </li>
  <% end %>
</ul>

